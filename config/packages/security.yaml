security:
    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        in_memory:
            memory:
                users:
                    HapKyd: { password: $2y$10$XOXnGqWrtjzLPM652Dww9ejxNAW.tDh8G/lgAe.UDw8pGj1uRhedW, roles: ROLE_ADMIN }
                    killlian: { password: $2y$10$tj9M2316TcUyR11fYtnuruh8e5R0Lg6MTlTxe3mv7FFxr2prDgfrO, roles: ROLE_USER }
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: true
            http_basic: true

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        # - { path: ^/admin, roles: ROLE_ADMIN }
        # - { path: ^/profile, roles: ROLE_USER }
        - { path: /ajouterEntreprise, roles: ROLE_ADMIN }
        - { path: /ajouterStage, roles: [ROLE_ADMIN, ROLE_USER] }
    
    encoders:
        Symfony\Component\Security\Core\User\User:
            algorithm: bcrypt
            cost:      10
